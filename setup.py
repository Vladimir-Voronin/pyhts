from setuptools import setup

setup(
    name='pyhts',
    version='1.0',
    description='module for HTS app (recognition and text handling)',
    author='Vladimir Voronin',
    packages=['pyhts'],  # same as name
    install_requires=[],  # external packages as dependencies
)
